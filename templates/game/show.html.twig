{% extends 'base.html.twig' %}

{% block body %}
<div>
    <h1>{{ game.name }}</h1>
    <img src={{ getImage(game.appid, game.img_icon_url) }} alt="{{ game.name }}">
    <!-- Playtime is in minutes, must be converted to "XhX" -->
    <p>{{ game.playtime_forever }}</p>
</div>

{% endblock %}
